"""Asset Compensation Hub.

The package keeps business rules independent from Flask, Excel, Word and
Outlook so each layer can be tested or replaced without rewriting the rest.
"""

__version__ = "0.1.0"
