# Claude Code project entry point

Read and follow `AGENTS.md`; it is the shared repository policy for Codex,
Claude Code and human contributors.

Read `PROJECT_CONTEXT.md` completely before starting work, then use
`docs/TEAM_DEVELOPMENT.md` for environment setup. Use synthetic demo data, work
on a feature branch, run the relevant checks, and leave production deployment
files unchanged unless the task explicitly concerns production deployment.
